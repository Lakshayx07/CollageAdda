import fetch from 'node-fetch';
const getToken = async () => {
  // wait, I don't need a valid token to see if the code crashes, but I need auth.
  // let's just make a mongoose query script directly!
}
